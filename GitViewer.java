// git stash push to save changes
// git checkout branch to switch to different branch
// git stash pop to pop previously saved changes
// git branch --show-current to get current branch
// git show-branch branch can check whether the branch exists

import java.nio.file.Path;

public class GitViewer {

  GitViewer(Process proc) {
  }

  // public boolean isGitRepository();
  // public static boolean isThereBranch();
  // public static boolean is
}
